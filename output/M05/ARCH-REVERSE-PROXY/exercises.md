# Практика к уроку 28

## Задание 1. Объясните роль reverse proxy

Опишите простыми словами, зачем Compliance может понадобиться reverse proxy.

В ответе упомяните:

- единый адрес системы;
- маршрутизацию frontend и API;
- связь с HTTPS;
- почему reverse proxy не заменяет backend-проверки.

## Задание 2. Разберите маршруты

Сначала объясните простыми словами, что в адресе `compliance.company.local/api/requests` является доменом, что является путем и почему это не папка на диске. Затем для адреса `compliance.company.local` предложите маршрутизацию и нарисуйте короткую схему потока от браузера к reverse proxy и backend-компонентам:

| Путь | Куда направить | Зачем |
| --- | --- | --- |
| `/` | | |
| `/api/requests` | | |
| `/api/reports` | | |
| `/health` | | |

## Задание 3. Опишите балансировку

Представьте, что у Compliance Backend запущены 3 экземпляра.

Опишите:

1. Что делает балансировщик.
2. Что должно случиться, если один экземпляр перестал отвечать.
3. Почему добавление нескольких экземпляров не решает все проблемы автоматически.

## Задание 4. Найдите требования за инфраструктурой

Перепишите инфраструктурные идеи как требования:

- единый корпоративный URL;
- HTTPS;
- несколько экземпляров backend;
- health check;
- понятная ошибка при недоступности сервиса.
